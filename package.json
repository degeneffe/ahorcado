{
  "name": "ahorcado",
  "version": "1.0.0",
  "description": "Juego del Ahorcado - PWA",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "build:apk": "bubblewrap build"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "jsdom": "^26.0.0",
    "serve": "^14.2.5",
    "vitest": "^3.0.0"
  }
}
